import PropTypes from 'prop-types'

const Cook = ({menu}) => {
    console.log(menu)
    return (
        <div>
            
        </div>
    );
};
Cook.propTypes ={
    menu:PropTypes.object
}

export default Cook;